import { ScreenShell } from "@/components/private/screen-shell"

export default function PlanPage() {
  return (
    <ScreenShell
      title="Plan"
      description="Weekly nutrition plan in clear read-only format."
    >
      This section will present the weekly structure in a simple way, optimized for quick comprehension and low friction.
    </ScreenShell>
  )
}
